{
  "name": "friday",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest",
    "test:run": "vitest run",
    "typecheck": "tsc --noEmit",
    "cargo:check": "cargo check --manifest-path src-tauri/Cargo.toml --locked",
    "cargo:test": "cargo test --manifest-path src-tauri/Cargo.toml --locked",
    "cargo:clippy": "cargo clippy --manifest-path src-tauri/Cargo.toml --locked -- -D warnings",
    "check": "npm run typecheck && npm run test:run && npm run build && npm run cargo:check && npm run cargo:test && npm run cargo:clippy",
    "tauri": "LLVM_PROFILE_FILE=/tmp/friday-%p.profraw tauri"
  },
  "dependencies": {
    "@ant-design/icons": "^6.0.1",
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-dialog": "^2",
    "antd": "^5",
    "react": "^19",
    "react-dom": "^19",
    "react-markdown": "^9",
    "remark-gfm": "^4"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "@testing-library/react": "^16.3.0",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "@vitejs/plugin-react": "^4",
    "jsdom": "^26.1.0",
    "typescript": "^5.7",
    "vite": "^6",
    "vitest": "^3.2.4"
  }
}
